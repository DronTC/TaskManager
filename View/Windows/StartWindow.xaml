<Window x:Class="TaskManager.View.Windows.StartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManager.View.Windows"
        mc:Ignorable="d"
        WindowStyle="None"
        Title="AuthWindow" Height="550" Width="400"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True" Background="Transparent" MouseDown="Window_MouseDown">
    <Border Background="White" CornerRadius="30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.1*"/>
                <RowDefinition Height="0.9*"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Content="x" Margin="0, 0, 15, 15"
                    Width="25" Height="25"
                    VerticalAlignment="Bottom" HorizontalAlignment="Right"
                    Style="{StaticResource SystemButton}"/>
            <Button Grid.Row="0" Content="—" Margin="0, 0, 50, 15"
                    Width="25" Height="25"
                    VerticalAlignment="Bottom" HorizontalAlignment="Right"
                    Style="{StaticResource SystemButton}"/>
            <Frame Grid.Row="1" Name="AuthFrame" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Border>
</Window>
